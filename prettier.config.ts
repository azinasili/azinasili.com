/**
 * Prettier will inherit settings from `.editorconfig` file. To reduce configuration
 * duplication please check there for code style settings.
 * @see https://prettier.io/docs/configuration
 */
const config = {
  arrowParens: 'avoid',
  bracketSameLine: false,
  bracketSpacing: true,
  jsxSingleQuote: false,
  htmlWhitespaceSensitivity: 'css',
  insertPragma: false,
  proseWrap: 'preserve',
  quoteProps: 'as-needed',
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
};

export default config;
